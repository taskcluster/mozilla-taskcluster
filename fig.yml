kueadmin:
  build: .
  command: |
    /bin/bash -c 'cd /app && ./node_modules/.bin/6to5-node -r /app/src/bin/kue production'
  volumes:
    - .:/app
  ports:
    - "60024:60024"
  links:
    - redis

test:
  build: .
  links:
    - redis

redis:
  image: redis
  ports:
    - "6379:6379"
